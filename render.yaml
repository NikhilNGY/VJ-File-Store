services:
  - type: worker
    name: Filestoree
    env: python:3.11        # Modern Python version
    plan: free               # Free plan
    buildCommand: |
      python -m pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: python bot.py
    autoDeploy: true         # Automatically deploy on new commits
